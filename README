File Renamer — масове перейменування файлів на Go

File Renamer — це консольна програма, створена для демонстрації роботи з пакетами, модулями та файловою системою в мові Go.
Вона дозволяє масово перейменовувати файли за різними правилами, підтримує dry run, undo та гнучкі шаблони пошуку.

📌 Можливості програми
✔ Основний функціонал:

Введення параметрів з консолі:

директорія

шаблон файлів (*.jpg, *.png, file_*.txt)

дія перейменування

додаткові параметри (залежно від дії)

Пошук файлів за шаблоном

Масове перейменування

Виведення результатів

Обробка помилок

✔ Підтримувані правила перейменування:
Правило	Опис
prefix	Додати префікс
suffix	Додати суфікс
replace	Замінити підрядок (формат: `old`)
extension	Змінити розширення
lowercase	Перевести ім'я у нижній регістр
uppercase	Перевести ім'я у верхній регістр
⭐ Бонусні можливості
Функція	Статус
Dry-run (попередній перегляд)	✔ Реалізовано
Undo (відкат змін) через backup файл	✔ Реалізовано
Підтримка регулярних виразів	✘ (не реалізовано, використовуються glob-шаблони)
Прогрес-бар	✘ (можна додати пізніше)
📂 Структура проєкту
file-renamer/
├── go.mod
├── main.go
├── config/
│   └── config.go
├── renamer/
│   └── renamer.go
├── model/
│   └── model.go
├── backup.json        # створюється автоматично
└── test_files/
    ├── IMG_1234.jpg
    ├── IMG_5678.jpg
    └── photo.png

Пакети:
config

– Зчитує параметри з консолі
– Валідовує введення

model

Спільні типи:

Rule — опис дії

RenameResult — результат перейменування

renamer

Пошук файлів

Застосування правил

Dry run

Реальне перейменування

Backup та undo

main

Інтерфейс (меню)

Взаємодія з користувачем

Виклик функцій пакетів

▶ Приклад роботи програми
=== File Renamer v1.0 ===

Що хочете зробити?
1. Перейменувати файли
2. Відкотити останні зміни (undo)
3. Вихід
Введіть номер дії: 1

Вкажіть директорію для перейменування: 
> ./test_files/

Вкажіть шаблон файлів (наприклад, *.png):
> *.png

Оберіть дію (prefix, suffix, replace, extension, lowercase, uppercase):
> prefix

Введіть значення для префікса:
> NEW_

🧪 Dry run:
=== Попередній перегляд змін (DRY RUN) ===
• IMG_1234.png → NEW_IMG_1234.png
• IMG_5678.png → NEW_IMG_5678.png
• photo.png → NEW_photo.png

🔄 Підтвердження:
Виконати перейменування? (y/n): y

📋 Результат:
=== Результат перейменування ===
• IMG_1234.png → NEW_IMG_1234.png
• IMG_5678.png → NEW_IMG_5678.png
• photo.png → NEW_photo.png

✅ Успішно перейменовано: 3 файлів з 3
💾 Backup збережено у backup.json

🔁 Undo (відкат змін)

При виборі пункту меню:

2. Відкотити останні зміни (undo)


Якщо backup.json є:

✅ Всі зміни успішно відкотені.

🛠 Як запустити
1. Клонувати репозиторій:
git clone https://github.com/yourname/file-renamer.git

2. Перейти в директорію:
cd file-renamer

3. Запустити програму:
go run .